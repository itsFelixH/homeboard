services:
  homeboard:
    build: .
    container_name: homeboard
    ports:
      - "7070:7070"
    volumes:
      - ./data:/app/data
      - ./config.local.yaml:/app/config.local.yaml:ro
    restart: unless-stopped
