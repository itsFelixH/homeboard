services:
  homeboard:
    build: .
    container_name: homeboard
    ports:
      - "7070:7070"
    volumes:
      - ./data:/app/data:ro
      - ./js/config.local.js:/app/js/config.local.js:ro
    restart: unless-stopped
